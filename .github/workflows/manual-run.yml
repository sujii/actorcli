name: Manual Workflow

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to deploy'
        required: true
        default: 'staging'
        type: choice
        options:
          - staging
          - production

jobs:
  berry:
    name: Setup Yarn/Berry
    runs-on: ubuntu-latest

    steps:
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '23'
          cache: 'yarn'

      - name: Enable Corepack
        run: corepack enable

      - name: Setup yarn/berry
        run: yarn init -2 && yarn set version stable

      - name: Install dependencies
        run: yarn install

  deploy:
    name: Deploy to ${{ github.event.inputs.environment }}
    runs-on: ubuntu-latest
    needs: berry

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '23'
          cache: 'yarn'

      - name: Install dependencies
        run: yarn install

      - name: Build Project
        run: yarn build

      - name: Deploy
        run: echo "Deploying to ${{ github.event.inputs.environment }}..."
